# Parser conformance fixtures

These binary RPUs are copied without modification from
`quietvoid/dovi_tool` revision
`38adec045bf183c24df38149836c920398072281`, directory `assets/tests`.
They are covered by the MIT license in `../LICENSE.libdovi.txt`.

The committed subset covers Profile 5, Profile 8.1, Profile 8.4, Profile 7
MEL/FEL, variable-length metadata, trailing bytes, and the deliberately
unsupported Profile 4 route. Tests must not regenerate expected values from
the wrapper under test.

